<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Link
        core:require="{handler: 'conditionalrules/ext/controller/TaskchainLinkHandler'}"
        text="{businessName}"
        enabled="{= !!${taskchainUrl} }"
        wrapping="true"
        press="handler.onTaskchainPress"/>

</core:FragmentDefinition>
